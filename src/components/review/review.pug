include ../like/like

mixin review({url,username,date,text,likes,theme})
    .review
        .review__dynamic
            img.review__img(src=url)
            .review__like
                +like({likes,theme})
        .review__description
            .review__user-name=username
            .review__date=date
            p.review__text=text