mixin type (items=[])
    .type 
        each item in items
            .type__item
                    div(class=item.class)
                        span.type__heading=item.heading
                        span.type__description=item.description